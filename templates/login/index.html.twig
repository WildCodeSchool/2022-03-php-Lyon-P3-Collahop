{% extends 'base.html.twig' %}

{% block body %}
	{% if error %}
		<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
	{% endif %}

	<body class="body_login">

		<header>
			{% include ('_navbar.html.twig') %}
		</header>

		{% include "home/partials/_sidebar_user.html.twig" %}

		<section class="section_login">
			<div class="container py-4">
				<div class="row d-flex justify-content-center align-items-center">
					<div class="col-12 col-md-8 col-lg-6 col-xl-5">
						<div class="card card_login text-white" style="border-radius: 1rem;">
							<div class="card-body p-5 text-center">
								<form action="{{ path('login') }}" method="post" class="mb-md-4 mt-md-4">
									<h2 class="fw-bold mb-5">Connexion</h2>

									<div class="form-outline form-white mb-5">
										<label class="form-label" for="username">Email</label>
										<input type="email" id="username" class="form-control form-control-lg" name="_username" value="{{ last_username }}" placeholder="Email" required/>
									</div>

									<div class="form-outline form-white mb-5">
										<label class="form-label" for="password">Mot de passe</label>
										<input type="password" id="password" class="form-control form-control-lg" name="_password" placeholder="Mot de passe" required/>
									</div>

									<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

									<button class="btn btn-lg px-5" type="submit">Connexion</button>
								</form>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<footer>
			{% include ('home/partials/_footer.html.twig') %}
		</footer>

	</body>
{% endblock %}
